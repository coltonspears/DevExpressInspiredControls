<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ControlTemplate x:Key="DxHorizontalProgressBarTemplate" TargetType="{x:Type ProgressBar}">
        <Border
            x:Name="PART_Track"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            CornerRadius="{DynamicResource DxCornerRadius}"
            ClipToBounds="True">
            <Grid x:Name="ProgressRoot">
                <VisualStateManager.VisualStateGroups>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Determinate" />
                        <VisualState x:Name="Indeterminate">
                            <Storyboard RepeatBehavior="Forever">
                                <DoubleAnimation
                                    Storyboard.TargetName="GlowTransform"
                                    Storyboard.TargetProperty="X"
                                    From="-90"
                                    To="360"
                                    Duration="0:0:1.4" />
                            </Storyboard>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateManager.VisualStateGroups>
                <Border
                    x:Name="PART_Indicator"
                    HorizontalAlignment="Left"
                    Background="{TemplateBinding Foreground}"
                    CornerRadius="{DynamicResource DxCornerRadius}" />
                <Rectangle
                    x:Name="PART_GlowRect"
                    Width="90"
                    HorizontalAlignment="Left"
                    Fill="{DynamicResource DxAccentBrush}"
                    Opacity="0.75">
                    <Rectangle.RenderTransform>
                        <TranslateTransform x:Name="GlowTransform" X="-90" />
                    </Rectangle.RenderTransform>
                </Rectangle>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsIndeterminate" Value="False">
                <Setter TargetName="PART_GlowRect" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="IsIndeterminate" Value="True">
                <Setter TargetName="PART_Indicator" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="ProgressRoot" Property="Opacity" Value="0.45" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="DxVerticalProgressBarTemplate" TargetType="{x:Type ProgressBar}">
        <Border
            x:Name="PART_Track"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            CornerRadius="{DynamicResource DxCornerRadius}"
            ClipToBounds="True">
            <Grid x:Name="ProgressRoot">
                <VisualStateManager.VisualStateGroups>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Determinate" />
                        <VisualState x:Name="Indeterminate">
                            <Storyboard RepeatBehavior="Forever">
                                <DoubleAnimation
                                    Storyboard.TargetName="GlowTransform"
                                    Storyboard.TargetProperty="Y"
                                    From="90"
                                    To="-360"
                                    Duration="0:0:1.4" />
                            </Storyboard>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateManager.VisualStateGroups>
                <Border
                    x:Name="PART_Indicator"
                    VerticalAlignment="Bottom"
                    Background="{TemplateBinding Foreground}"
                    CornerRadius="{DynamicResource DxCornerRadius}" />
                <Rectangle
                    x:Name="PART_GlowRect"
                    Height="90"
                    VerticalAlignment="Bottom"
                    Fill="{DynamicResource DxAccentBrush}"
                    Opacity="0.75">
                    <Rectangle.RenderTransform>
                        <TranslateTransform x:Name="GlowTransform" Y="90" />
                    </Rectangle.RenderTransform>
                </Rectangle>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsIndeterminate" Value="False">
                <Setter TargetName="PART_GlowRect" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="IsIndeterminate" Value="True">
                <Setter TargetName="PART_Indicator" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="ProgressRoot" Property="Opacity" Value="0.45" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="DxProgressBarStyle" TargetType="{x:Type ProgressBar}">
        <Setter Property="Background" Value="{DynamicResource DxSurfaceSubtleBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DxBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Foreground" Value="{DynamicResource DxAccentBrush}" />
        <Setter Property="Height" Value="{DynamicResource DxProgressBarHeight}" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="MinWidth" Value="80" />
        <Setter Property="Template" Value="{StaticResource DxHorizontalProgressBarTemplate}" />
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Height" Value="80" />
                <Setter Property="MinWidth" Value="0" />
                <Setter Property="Width" Value="{DynamicResource DxProgressBarHeight}" />
                <Setter Property="Template" Value="{StaticResource DxVerticalProgressBarTemplate}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type ProgressBar}" BasedOn="{StaticResource DxProgressBarStyle}" />
</ResourceDictionary>
