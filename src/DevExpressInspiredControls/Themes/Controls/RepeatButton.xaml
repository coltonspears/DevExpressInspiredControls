<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="DxRepeatButtonStyle" TargetType="{x:Type RepeatButton}">
        <Setter Property="Background" Value="{DynamicResource DxSurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DxBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Delay" Value="400" />
        <Setter Property="Foreground" Value="{DynamicResource DxTextBrush}" />
        <Setter Property="FontFamily" Value="{DynamicResource DxFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource DxFontSize}" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource DxFocusVisualStyle}" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="Interval" Value="75" />
        <Setter Property="MinHeight" Value="{DynamicResource DxControlHeight}" />
        <Setter Property="Padding" Value="{DynamicResource DxControlPadding}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border
                        x:Name="Chrome"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{DynamicResource DxCornerRadius}"
                        SnapsToDevicePixels="True">
                        <ContentPresenter
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            RecognizesAccessKey="True"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource DxSurfaceHoverBrush}" />
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource DxAccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource DxAccentPressedBrush}" />
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource DxAccentPressedBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource DxOnAccentTextBrush}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource DxAccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource DxDisabledBrush}" />
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource DxBorderBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource DxTextDisabledBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type RepeatButton}" BasedOn="{StaticResource DxRepeatButtonStyle}" />
</ResourceDictionary>
