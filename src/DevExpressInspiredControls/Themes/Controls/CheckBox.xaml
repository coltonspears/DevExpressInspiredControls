<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="DxCheckBoxStyle" TargetType="{x:Type CheckBox}">
        <Setter Property="Background" Value="{DynamicResource DxSurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DxBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="{DynamicResource DxTextBrush}" />
        <Setter Property="FontFamily" Value="{DynamicResource DxFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource DxFontSize}" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource DxFocusVisualStyle}" />
        <Setter Property="MinHeight" Value="{DynamicResource DxControlHeight}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CheckBox}">
                    <Grid Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border
                            x:Name="MarkBorder"
                            Width="{DynamicResource DxIndicatorSize}"
                            Height="{DynamicResource DxIndicatorSize}"
                            VerticalAlignment="Center"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="1">
                            <Grid>
                                <Path
                                    x:Name="CheckMark"
                                    Width="10"
                                    Height="8"
                                    Data="M 1 4 L 4 7 L 9 1"
                                    SnapsToDevicePixels="True"
                                    Stroke="{DynamicResource DxOnAccentTextBrush}"
                                    StrokeEndLineCap="Square"
                                    StrokeStartLineCap="Square"
                                    StrokeThickness="2"
                                    Visibility="Collapsed" />
                                <Rectangle
                                    x:Name="IndeterminateMark"
                                    Width="8"
                                    Height="2"
                                    Fill="{DynamicResource DxOnAccentTextBrush}"
                                    Visibility="Collapsed" />
                            </Grid>
                        </Border>
                        <ContentPresenter
                            Grid.Column="1"
                            Margin="7,0,0,0"
                            VerticalAlignment="Center"
                            ContentSource="Content"
                            RecognizesAccessKey="True" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="MarkBorder" Property="BorderBrush" Value="{DynamicResource DxAccentBrush}" />
                            <Setter TargetName="MarkBorder" Property="Background" Value="{DynamicResource DxSurfaceHoverBrush}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="MarkBorder" Property="Background" Value="{DynamicResource DxAccentBrush}" />
                            <Setter TargetName="MarkBorder" Property="BorderBrush" Value="{DynamicResource DxAccentBrush}" />
                            <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="{x:Null}">
                            <Setter TargetName="MarkBorder" Property="Background" Value="{DynamicResource DxAccentBrush}" />
                            <Setter TargetName="MarkBorder" Property="BorderBrush" Value="{DynamicResource DxAccentBrush}" />
                            <Setter TargetName="IndeterminateMark" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="MarkBorder" Property="BorderBrush" Value="{DynamicResource DxAccentPressedBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="MarkBorder" Property="Background" Value="{DynamicResource DxDisabledBrush}" />
                            <Setter TargetName="MarkBorder" Property="BorderBrush" Value="{DynamicResource DxBorderBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource DxTextDisabledBrush}" />
                            <Setter TargetName="CheckMark" Property="Stroke" Value="{DynamicResource DxTextDisabledBrush}" />
                            <Setter TargetName="IndeterminateMark" Property="Fill" Value="{DynamicResource DxTextDisabledBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type CheckBox}" BasedOn="{StaticResource DxCheckBoxStyle}" />
</ResourceDictionary>
